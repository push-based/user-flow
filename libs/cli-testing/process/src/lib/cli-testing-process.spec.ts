import { cliTestingProcess } from './cli-testing-process';

describe('cliTestingProcess', () => {
  it('should work', () => {
    expect(cliTestingProcess()).toEqual('cli-testing-process');
  });
});
