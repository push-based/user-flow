import { cliTestingCliProject } from './cli-testing-cli-project';

describe('cliTestingCliProject', () => {
  it('should work', () => {
    expect(cliTestingCliProject()).toEqual('cli-testing-cli-project');
  });
});
