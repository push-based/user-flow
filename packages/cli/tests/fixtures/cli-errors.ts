export const ERROR_URL_REQUIRED = 'URL is required. Either through the console as `--url` or in the `.user-flow.json`';
export const ERROR_UF_PATH_REQUIRED = (path: string) => `Error: ufPath: ${path} is no directory`;
export const ERROR_UF_PATH_NO_DIR = (dir: string) => 'Error: ufPath: ${dir} is no directory';
