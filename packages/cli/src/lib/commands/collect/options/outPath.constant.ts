export const DEFAULT_PERSIST_OUT_PATH = './measures';
export const PROMPT_PERSIST_OUT_PATH =
  'What is the directory to store results in?';
export const ERROR_PERSIST_OUT_PATH_REQUIRED =
  'Path to output folder is required. Either through the console as `--outPath` or in the `.user-flowrc.json`';
