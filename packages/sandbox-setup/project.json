{
  "root": "packages/sandbox-setup",
  "sourceRoot": "packages/sandbox-setup/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/sandbox-setup/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/packages/sandbox-setup"],
      "options": {
        "jestConfig": "packages/sandbox-setup/jest.config.js",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
